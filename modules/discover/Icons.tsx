import styled from "styled-components";
import {
  borders,
  height,
  width,
  minHeight,
  maxWidth,
  space,
  position,
  top,
  right,
  bottom,
  left,
  zIndex
} from "styled-system";

import Icon from "../traveling/Icon/Icon";

//Icons
export const TravelIcon = styled(Icon)`
${height}
${width}
${space}
`;

export const ExploreIcon = styled(Icon)`
${height}
${width}
${space}
`;

export const SavedIcon = styled(Icon)`
${height}
${width}
${space}
`;

export const ChatIcon = styled(Icon)`
${height}
${width}
${space}
`;

export const ProfileIcon = styled(Icon)`
${height}
${width}
${space}
`;

export const ActivityIcon = styled(Icon)`
${height}
${width}
${space}
`;

export const SearchIcon = styled(Icon)`
${height}
${width}
${space}
`;

export const DotIcon = styled(Icon)`
${height}
${width}
${space}
`;
